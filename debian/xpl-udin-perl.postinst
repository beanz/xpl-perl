#!/bin/sh
set -e

test "$1" = 'configure' || exit 0

if test -e /etc/service/xpl-udin; then
  echo 'Restarting xpl-udin service...'
  svc -t /etc/service/xpl-udin || :
else
  update-service --add /etc/sv/xpl-udin
fi

test -z "$2" || exit 0

chown xplperllog:adm /var/log/xpl-perl/xpl-udin
